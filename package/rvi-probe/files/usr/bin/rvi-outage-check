#!/bin/sh
set -eu
WORKER_URL=$(uci -q get rviprobe.config.worker_url)
[ -z "$WORKER_URL" ] && { echo "Worker URL not set" >&2; exit 1; }
curl -fsS "$WORKER_URL?fn=status" || echo '{"status":"unknown"}'
